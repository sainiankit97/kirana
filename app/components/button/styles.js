import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: 'rgb(201,89,55)',
    padding: 15,
    marginTop: 20,
    borderRadius: 12,
  },
  buttonText: {
    fontSize: 16,
    lineHeight: 24,
    textAlign: 'center',
    color: 'white',
    fontWeight: '600',
  },
});
